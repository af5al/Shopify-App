To start app

- clone repository from github to a folder
- "cd new folder"
- enter "npm i" tto install the required dependencies
- if shopify-cli is not installed, 
  enter "npm install -g @shopify/cli@latest"
